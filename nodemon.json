{
	"watch": ["src"],
	"ext": "ts,js,json",
	"ignore": ["node_modules", "dist", "generated", "*.test.ts", "*.spec.ts"],
	"exec": "ts-node --transpile-only -r tsconfig-paths/register src/server.ts",
	"env": {
		"NODE_ENV": "development"
	},
	"delay": 500,
	"verbose": true
}
